{
  "name": "jaring-metal-ai",
  "version": "1.0.0",
  "description": "AI-powered quotation intelligence platform for Jaring Metal",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-deps": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
